<script>
    const subtitulo=document.querySelector('.subtitulo');

    const observador= new IntersectionObserver((entries)=>{
        entries.forEach(entry=>{
            if (entry.isIntersecting) {
                entry.target.classList.add('show2')
            }
        })
    },{
        threshold:0
    })
        observador.observe(subtitulo)

        setInterval(() => {
            subtitulo.classList.toggle('colors')
        },6000);

</script>




<div class="banner h-[20rem] p-5 md:h-auto md:p-16  flex flex-col items-center justify-center overflow-hidden  sticky top-0 z-30">
    <a href="/" rel="noopener noreferrer"><h1 class="titulo cursor-pointer font-[900] text-6xl  text-center lg:text-8xl text-white"> NOE TRIMINIO</h1></a>
    <h2 class="w-[70%] mx-auto subtitulo font-[500] text-xl  text-center md:text-3xl lg:text-5xl opacity-0">JUNIOR FULLSTACK</h2>
</div>



<style>
    .subtitulo{
        color: transparent;
        background: rgb(234, 245, 17);
        background: linear-gradient(122deg, rgb(255, 221, 0) 31%, rgba(255,0,170,1) 68%); 
        background-clip: text;
        transition: all .4s ease;
}
.show2{
    opacity: 1;
    transition: all .6s ease;
    transition-delay: 1s;
}
.colors{
    color: transparent;
        background: rgb(17,242,245);
        background: linear-gradient(122deg, rgb(0, 187, 255) 31%, rgba(255,0,170,1) 68%); 
        background-clip: text;
        transition: all .4s ease;  
}
.banner{
   background-image: url(/img/wave.svg);
   background-size: cover;
   background-repeat: no-repeat;
  
}

    
</style>